export type BbConfig = {
  email: string;
  apiToken: string;
  workspace: string;
  repoSlug: string;
};

export type MaskedBbConfig = {
  email: string;
  apiToken: string;
  workspace: string;
  repoSlug: string;
};
